{
  "name": "e2e-visual-parity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Pixel + computed-style parity harness comparing the impower-dev Preact port against the web-component baseline (../impower on main).",
  "scripts": {
    "parity": "playwright test",
    "parity:smoke": "playwright test --grep @smoke",
    "parity:report": "playwright show-report report/playwright",
    "parity:validate-allowlist": "tsx scripts/validate-allowlist.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "@types/pngjs": "^6.0.5",
    "cross-env": "^7.0.3",
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0",
    "tsx": "^4.19.2",
    "yaml": "^2.6.1"
  }
}
