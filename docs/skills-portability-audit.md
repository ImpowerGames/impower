# Skills portability audit

Audit baseline: `1979ceb4fc65921b78615646d2da9b26d43f5f02` from current `origin/main`. Issues #496, #494 and #505 were read in full before implementation. #495 is merged. The index contains nine skills and nineteen standalone checks under the harness directory. Driver preflight passed disk, Chromium launch, GitHub authentication, repository detection and executable dependencies; disk headroom was 86.3 GB in the driver report. Git has `core.symlinks=false` on this Windows checkout.

## Decisions from execution-path inspection

| Area | Finding | Required implementation and evidence |
| --- | --- | --- |
| Discovery | Skills have one harness-specific source and no project agent entry point for another runner. | Move all nine skills with Git to `.agents/skills`; install ignored junctions on Windows and symlinks on POSIX. Keep one shared instructions source read from both entry points. Verify discovery in fresh headless sessions. |
| Link installation | Git cannot materialize junctions with the checkout settings. | A bare Node postinstall creates links, validates the target and refuses populated directories, foreign links and unsafe parents. Exercise idempotence and target preservation in scratch repositories on both platforms. |
| CLI dispatch | Both browser drivers compare the lexical command path with the resolved module path. A linked invocation can silently execute no command. | Compare real paths for CLI dispatch and exercise nonempty status output through each link. |
| Runtime state | Drivers derive their root at the same directory depth, but cleanup and cross-worktree download guards spell one layout. Existing worktrees can still own live servers in that layout. | Use canonical state paths and retain explicit migration lookup for existing worktrees, without probing the same linked driver twice. Test fallback and canonical/link equivalence. |
| Capabilities | Skill invocation, file writing, session naming, private artifacts and review spawning name harness tools. | State capabilities in shared skills; keep concrete mappings in one runner note. Missing required capabilities block that step explicitly; optional naming may be skipped. |
| Hook coverage | Typed issue creation, generated-language edits and the shared stash require actual tool interception; safe artifact writing is a prose rule. | Share decision functions under `.agents/hooks/`, retain harness definitions and thin entry points in their own directories, and verify both harnesses execute the guards. Codex project hooks need explicit trust; shared instructions alone do not establish enforcement. |
| Review identity | Shared review text selects pinned models and fallback families and assumes runtime introspection. | Require caller-supplied writer identity, reviewer route and invocation method; distinguish configured route from independently reported identity; abort missing or matching identities and disclose unavailable introspection. |
| Review execution | The skill assumes parallel subagents, parent termination and that absent comments mean stopped reviewers. | Support fresh serial sessions with identical diff and prompt; wait for process exit and verify paginated comments before adjudicating or writing. Durable launch records prevent duplicate relaunch and support automatic implement/review handoffs. |
| Reviewer definitions | The two model definitions duplicate stance, probe restrictions and reporting instructions. | Generate minimal pin/abort definitions from one runner configuration; put the review contract in the shared prompt. Check generation and allowed exceptions. |
| Shell and filesystem | Generic bash resolves to unavailable WSL on this machine. `resolve-issue` recommends recursive deletion despite junction risk. | Resolve Git for Windows bash explicitly; replace the cleanup suggestion with the guarded cleaner and scratch-only experiments. Keep one vitest process, one fork and a 1 GB heap. |
| POSIX acceptance (#505) | Current tests skip process matching and ignored-link assertions. The matcher unconditionally folds case and backslashes, including on POSIX. | Assert native path semantics on each platform, preserve Windows coverage, assert actual POSIX symlink status and retain external-target sentinels. Demonstrate red/green and complete the small-ticket resolve workflow on the combined PR. |
| CI and checks | Discovery and sparse checkout cover only the harness tree. Several checks use literal paths and mutation strings. | Update paths and discovery together; run shared, hook and installer checks on Windows and Linux. Derive totals and fail empty/missing/unsupported checks. Add mutation controls for forbidden references and execution assumptions. |
| Feedback | The ninth skill archives durable problem identities and runner session references. | Move its scripts and references intact; preserve archive parsing and reporting. Report concrete friction through the standing inbox with a stable session identity. |

## Verification plan

Run installer and deletion probes only in printed scratch repositories. Verify postinstall, links and actual CLI dispatch, fresh discovery in both installed harnesses, a serial review round with comments and adjudications, and #505's resolve workflow. Record exact commits, process exits, configured and reported reviewer identities, comment IDs and CI runs on the combined PR. Text inspection alone does not establish compatibility. Retain any unverified environment limitation in the evidence and PR notes.

## Execution evidence

Fresh Windows sessions discovered all nine skills in their native catalogs: Claude Code 2.1.220 session `2d23fc6d-8e5a-473b-b39c-3aa47b7cbc40` and Codex CLI 0.153.4 session `01a08ef5-625f-73a0-911b-6f26065ebd9a`. Both read the shared instructions and review/resolve skills. Claude's generic Glob returned no entries through an ignored junction; native discovery and canonical file reads worked, so runner notes describe that distinction. A fresh full workspace npm install ran postinstall successfully, creating all three Windows junctions. The new worktree then passed all five driver preflight checks with 83.4 GB free.

#505 acceptance used the combined resolve-issue workflow: issue read, preflight, branch from current main, a failing POSIX matching assertion against the old matcher, implementation, standalone checks, PR #521 and the serial review/adjudication below. Linux tooling CI passed on `f8ba8c750f9c1cf891936048e34646154e6340ab` (run `34567134511`), including actual POSIX link and process-path assertions. Windows CI exposed an 8.3 temporary-path fixture discrepancy; the scratch fixture now resolves its real native path before comparing Git's output. Final CI and acceptance completion are recorded on the PR.

The first serial prompt attempt stopped without a report because a duplicated placeholder altered its own identity guard. It never handed off. The corrected round reviewed `f8ba8c750f9c1cf891936048e34646154e6340ab`: Claude reviewer PID 38924 exited successfully at `2026-09-11T06:02:08.427Z`; its report is [comment 5630185733](https://github.com/ImpowerGames/impower/pull/521#issuecomment-5630185733). After exit and comment verification, the handoff runner automatically started Codex adjudicator PID 8576 at `06:02:09.696Z`, which exited successfully at `06:05:36.645Z` and posted [adjudication 5630213542](https://github.com/ImpowerGames/impower/pull/521#issuecomment-5630213542). Every finding received a disposition. No user nudge or overlapping writer was needed. The caller supplied `gpt-6-astra` and `claude-opus-5`; independent backend model attestation was unavailable and is not claimed.

## Shared hook execution evidence

On 2026-09-11, fresh Codex CLI 0.153.4 session `01a090e3-e637-7a23-9197-04bd118a0565` and Claude Code 2.1.220 session `2b306e11-3825-48ee-8da0-ff7530a32806` each attempted five actual tool calls using copies of the shared source and the committed harness definitions. Both blocked stash mutation, untyped issue creation and a protected generated-file edit; both allowed a typed issue command and an ordinary file edit. Shell commands shadowed Git and GitHub CLI with inert local functions, so even a failed guard could not touch a stash or create an issue. The allowed file contained `allowed`; the protected file remained absent in both repositories.

The printed scratch repositories were `C:/Users/Lovelle/AppData/Local/Temp/impower-496-hooks-codex-bmCxhN` and `C:/Users/Lovelle/AppData/Local/Temp/impower-496-hooks-claude-sfj2UN`. Codex PID 5372 exited 0 at `14:35:07.209Z`; Claude PID 1960 exited 0 at `14:35:40.380Z`. The coordinator awaited each exit. Full local event logs are `impower-496-hooks-codex-live-2.jsonl` and `impower-496-hooks-claude-live-2.jsonl` in that same Temp directory. Codex's log includes explicit `Command blocked by PreToolUse hook` errors; Claude's includes error tool results carrying the policy reasons. These are tool-interception results, not model promises to obey instructions.

The Codex scratch invocation used `--dangerously-bypass-hook-trust` after inspection of the copied hook sources, plus a command-local trusted-project setting. It changed no persistent trust store. This proves execution with vetted hooks enabled; normal users must review the installed definitions through `/hooks`, including after changes. It does not prove automatic trust during postinstall. POSIX configuration and Windows `commandWindows` are exercised from a nested cwd by the standalone policy check; live harness interception was verified on Windows. Safe artifact writing remains an instruction, and the static-analysis and tool-coverage limits are listed in runner notes.

Round 4's working-directory concern was checked through actual Codex session `01a090fd-576c-7140-8a06-cd585cd353fa`. Its session root was `C:/Users/Lovelle/AppData/Local/Temp/impower-496-hook-workdir-hUTUuF`. A nested foreign Git repository contained an inert replacement hook; a second target was a directory outside any repository. The actual tool-call arguments selected those two `workdir` values. Both inert stash calls were blocked by the session's shared-stash policy, and neither foreign-hook nor command sentinel executed. PID 11308 exited 0 at `15:02:18.682Z`; `impower-496-hook-workdir-live.jsonl` contains both runtime denials. The hook process uses the session cwd, not the command's workdir. Running its shell string manually from another directory does not model that lifecycle.

After the final wrapper corrections, both full five-operation probes passed again: Codex session `01a09102-1ab5-7ff0-9c9a-22677a7fe2cf`, PID 15544, exited 0 at `15:07:54.998Z`; Claude session `de68f274-e689-46db-8fcd-5323e4503a6a`, PID 24068, exited 0 at `15:08:28.655Z`. Scratch roots ended in `impower-496-hooks-codex-oQqfuS` and `impower-496-hooks-claude-1gw0Jk`; local logs are `impower-496-hooks-codex-live-final.jsonl` and `impower-496-hooks-claude-live-final.jsonl`. Both allowed files were read back and both protected files remained absent. Codex session `01a09103-513f-70a3-b14a-7e46f3b79526` simulated a missing runtime by substituting an unavailable executable into a scratch copy of the Windows hook command. Its actual shell call was blocked with a repository-hook reason; PID 24284 exited 0 at `15:08:44.705Z`. The original native command returned exit 1 rather than the required blocking exit 2; the corrected wrapper and its regression check require exit 2. The log is `impower-496-hooks-codex-missing-live-final.jsonl`.

That review led to explicit migration recovery, link-only repair tests, legacy state coverage for both drivers, lock cleanup on initialization failure, a validated prompt builder, restored check-count and skipped-case reporting, and removal of an unsupported large-suite memory claim. Populated legacy directories still require preservation and reconciliation: postinstall intentionally cannot decide which personal artifacts to discard. Scratch checks cover moved-checkout link repair and external target preservation. Existing POSIX coverage gaps tracked in #506, #507 and #508 remain visible in check summaries; this work does not claim complete browser-driver parity across operating systems.
