{
  "name": "@impower/definitions",
  "displayName": "Impower Definitions",
  "description": "YAML definition files that are converted to json and propagated to other modules.",
  "version": "0.0.1",
  "scripts": {
    "language": "tsx src/language.ts ../packages/sparkdown/language ../vscode-sparkdown/language",
    "build": "npm run language"
  },
  "dependencies": {
    "tsx": "^3.12.7",
    "yaml": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.1"
  }
}
