import styled from "@emotion/styled";

const StyledButtonChildrenArea = styled.div`
  pointer-events: none;
  touch-action: inherit;
  position: absolute;
  bottom: 0;
  right: 0;
`;

export default StyledButtonChildrenArea;
