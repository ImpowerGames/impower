import { SparkStructType } from "../types/SparkStructType";

export const structTypes: readonly SparkStructType[] = [
  "map",
  "ui",
  "style",
  "camera",
  "entity",
  "character",
  "animation",
  "writer",
  "sound",
] as const;
