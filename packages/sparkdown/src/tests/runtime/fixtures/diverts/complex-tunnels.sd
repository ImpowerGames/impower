-> one(1) -> two(2) ->
three (3)

scene one(num)
  one ({num})
  -> oneAndAHalf(1.5) ->
  ->->

end
scene oneAndAHalf(num)
  one and a half ({num})
  ->->

end
scene two(num)
  two ({num})
  ->->
end
