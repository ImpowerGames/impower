screen form with
  field @input={ name = event.value }
  checkbox @change={ muted = event.checked }
  button "Reset" @click={ score = 0; combo = 0 }
  field @input={ player.name = event.value }
  button "Go" @click={ start() }
end
