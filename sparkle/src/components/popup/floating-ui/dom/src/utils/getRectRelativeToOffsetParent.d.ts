import type { Rect, Strategy, VirtualElement } from "../../../core";
export declare function getRectRelativeToOffsetParent(
  element: Element | VirtualElement,
  offsetParent: Element | Window,
  strategy: Strategy
): Rect;
