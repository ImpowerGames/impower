import { LayerName } from "../types/layerName";

/**
 * Layer names that are supported by default.
 *
 * @summary LayerName
 */
export const DEFAULT_LAYERS: LayerName[] = [
  "drawer",
  "dialog",
  "dropdown",
  "alert",
  "tooltip",
];
